#!/bin/bash

loc="$PWD"
cd /tmp/

WARN=${PYTHONWARNINGS}
export PYTHONWARNINGS="ignore"

ndmg_bids /tmp/small_demo/outputs/graphs/ /tmp/small_demo/outputs/qc group

export PYTHONWARNINGS=${WARN}

echo "Path to qc fig: /tmp/small_demo/outputs/qc/desikan/plot.html"
cd "$loc"
