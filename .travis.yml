virtualenv:
  system_site_packages: true

language: python

python:
    - "2.7"

services:
    - docker

before_install:
    - docker pull neurodata/ndmg_dev:latest

install:
    - pip install -r requirements.txt

script:
    - pytest
