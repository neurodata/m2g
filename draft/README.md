In order to effectively access the overleaf latex files, you must do the following:

- clone this repo
- cd to this directory
- unzip the `overleaf.zip` archive
- cd into the `overleaf` directory
- `git pull` to get most recent changes

Happy writing!
